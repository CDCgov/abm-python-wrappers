[tool.poetry]
name = "abmwrappers"
version = "0.1.0"
description = ""
authors = ["Chris Valleau, William Koval"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
PyYAML = "^6.0.1"
pre-commit = "^3.7.1"
scipy = "^1.14.0"
sobol-seq = "^0.2.0"
cfa-azure = {git = "https://github.com/CDCgov/cfa_azure.git"}
polars = "^1.6.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
